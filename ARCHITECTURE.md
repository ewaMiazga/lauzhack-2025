# 🎨 SATUK VLM - Visual Architecture

**Satellite AI for Terrain Understanding & Knowledge**

## 📐 System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                         USER'S BROWSER                               │
│                                                                      │
│  ┌────────────────────────────────────────────────────────────┐   │
│  │                    index.html                               │   │
│  │  ┌──────────────────────────────────────────────────┐     │   │
│  │  │           🌍 Interactive Map                      │     │   │
│  │  │         (Leaflet.js + Draw Tools)                │     │   │
│  │  │                                                   │     │   │
│  │  │  👆 User draws rectangle to select region        │     │   │
│  │  └──────────────────────────────────────────────────┘     │   │
│  │                                                            │   │
│  │  ┌──────────────────┐  ┌────────────────────────────┐   │   │
│  │  │  Control Panel   │  │  Analysis Section          │   │   │
│  │  │  ├─ Date Range   │  │  ├─ Prompt Input          │   │   │
│  │  │  ├─ Layer Toggle │  │  ├─ Analyze Button        │   │   │
│  │  │  └─ Fetch Button │  │  └─ Chat Interface        │   │   │
│  │  └──────────────────┘  └────────────────────────────┘   │   │
│  │                                                            │   │
│  │  Styled by: styles.css                                    │   │
│  │  Logic in: app.js                                         │   │
│  └────────────────────────────────────────────────────────────┘   │
│                                                                      │
└──────────────────────────┬───────────────────────────────────────────┘
                           │
                           │ HTTP Requests (JSON)
                           │ POST /api/fetch-data
                           │ POST /api/analyze
                           │
┌──────────────────────────▼───────────────────────────────────────────┐
│                    FLASK BACKEND SERVER                              │
│                      (backend.py)                                    │
│                    Port: 5000                                        │
│                                                                      │
│  ┌────────────────────────────────────────────────────────────┐   │
│  │                   API Endpoints                             │   │
│  │                                                             │   │
│  │  GET  /                    → Serve index.html             │   │
│  │  GET  /api/health          → Server status                │   │
│  │  POST /api/fetch-data      → Get satellite images         │   │
│  │  POST /api/analyze         → AI vision analysis           │   │
│  │  GET  /api/test-car-image  → Test vision API              │   │
│  └────────────────────────────────────────────────────────────┘   │
│                                                                      │
│  ┌────────────────────────────────────────────────────────────┐   │
│  │              Core Functions                                 │   │
│  │                                                             │   │
│  │  encode_image_to_base64()  → Convert image to base64      │   │
│  │  analyze_image_with_vlm()  → Call Together AI             │   │
│  └────────────────────────────────────────────────────────────┘   │
│                                                                      │
└───────────┬────────────────────────────┬─────────────────────────────┘
            │                            │
            │                            │
            ▼                            ▼
┌───────────────────────┐    ┌──────────────────────────────┐
│   File System         │    │   Together AI API            │
│                       │    │   (Cloud Service)            │
│  📁 satellite_data/   │    │                              │
│     └─ *.jpg          │    │  Model:                      │
│                       │    │  meta-llama/                 │
│  📁 images/           │    │  Llama-Vision-Free           │
│     └─ car.jpg        │    │                              │
│                       │    │  Input: base64 image +       │
│  📄 .env              │    │         text prompt          │
│     TOGETHER_API_KEY  │    │                              │
│                       │    │  Output: Streaming text      │
└───────────────────────┘    │          analysis            │
                             └──────────────────────────────┘
```

## 🔄 Request/Response Flow

### Scenario: User asks "What is the burn severity?"

```
┌─────────┐
│ STEP 1  │ User draws rectangle on map
└────┬────┘
     │
     ▼
┌─────────────────────────────────────────┐
│ app.js captures coordinates:            │
│ {north: 46.0, south: 45.5,             │
│  east: 13.5, west: 13.0}               │
└────┬────────────────────────────────────┘
     │
     ▼
┌─────────┐
│ STEP 2  │ User clicks "Fetch Satellite Data"
└────┬────┘
     │
     ▼
┌─────────────────────────────────────────┐
│ app.js → POST /api/fetch-data           │
│ Payload: {region, dateRange, layers}   │
└────┬────────────────────────────────────┘
     │
     ▼
┌─────────────────────────────────────────┐
│ backend.py checks satellite_data/       │
│ Returns: {images: ["sentinel2_...jpg"]}│
└────┬────────────────────────────────────┘
     │
     ▼
┌─────────┐
│ STEP 3  │ User types prompt & clicks "Analyze"
└────┬────┘
     │
     ▼
┌─────────────────────────────────────────┐
│ app.js → POST /api/analyze              │
│ Payload: {                              │
│   prompt: "What is burn severity?",    │
│   region: {...},                        │
│   dateRange: {...},                     │
│   layers: [...]                         │
│ }                                        │
└────┬────────────────────────────────────┘
     │
     ▼
┌─────────────────────────────────────────┐
│ backend.py:                             │
│ 1. Loads satellite image                │
│ 2. Encodes to base64                    │
│ 3. Enhances prompt with context         │
└────┬────────────────────────────────────┘
     │
     ▼
┌─────────────────────────────────────────┐
│ Together AI Vision API Call:            │
│                                          │
│ messages = [{                            │
│   "role": "user",                        │
│   "content": [                           │
│     {"type": "text", "text": prompt},   │
│     {"type": "image_url",               │
│      "image_url": {                     │
│        "url": "data:image/jpeg;         │
│                base64,{encoded}"        │
│      }}                                  │
│   ]                                      │
│ }]                                       │
└────┬────────────────────────────────────┘
     │
     ▼
┌─────────────────────────────────────────┐
│ AI analyzes image:                      │
│ - Identifies burn patterns              │
│ - Assesses vegetation health            │
│ - Estimates severity                    │
│                                          │
│ Streams response back...                │
└────┬────────────────────────────────────┘
     │
     ▼
┌─────────────────────────────────────────┐
│ backend.py collects streamed chunks     │
│ Returns: {                               │
│   success: true,                        │
│   response: "Based on the satellite..." │
│ }                                        │
└────┬────────────────────────────────────┘
     │
     ▼
┌─────────────────────────────────────────┐
│ app.js displays in chat interface       │
│                                          │
│ 🤖 SATUK VLM:                           │
│ "Based on the satellite imagery, the    │
│  burn severity appears to be moderate   │
│  to high in the selected region..."     │
└─────────────────────────────────────────┘
```

## 📦 Component Breakdown

### Frontend Components (in browser):

```
index.html (Structure)
├── Header
│   └── Logo + Title
├── Main Content (CSS Grid)
│   ├── Map Section
│   │   ├── Leaflet Map
│   │   └── Drawing Tools
│   └── Control Panel
│       ├── Date Selector
│       ├── Layer Toggles
│       ├── Region Info
│       └── Fetch Button
└── Analysis Section
    ├── Prompt Input
    ├── Submit Button
    └── Chat Display

app.js (Logic)
├── State Management
│   └── appState object
├── Map Initialization
│   ├── initMap()
│   └── Drawing event handlers
├── UI Updates
│   ├── updateRegionInfo()
│   └── updateFetchButton()
└── API Communication
    ├── handlePromptSubmission()
    └── fetch() calls

styles.css (Design)
├── Color Variables
├── Layout (Grid + Flex)
├── Component Styles
│   ├── .header
│   ├── .map-section
│   ├── .control-panel
│   └── .analysis-section
└── Animations
    ├── fadeIn
    └── loading-dots
```

### Backend Components (Flask server):

```
backend.py
├── Flask App Setup
│   ├── CORS enabled
│   └── Static file serving
├── Configuration
│   ├── SATELLITE_DATA_DIR
│   └── IMAGES_DIR
├── Helper Functions
│   ├── encode_image_to_base64()
│   └── analyze_image_with_vlm()
└── API Routes
    ├── / (serve index.html)
    ├── /api/health
    ├── /api/fetch-data
    ├── /api/analyze
    └── /api/test-car-image
```

## 🔑 Key Technologies

```
┌─────────────────────────────────────────┐
│           Frontend Stack                │
├─────────────────────────────────────────┤
│ HTML5      → Structure                  │
│ CSS3       → Styling + Animations       │
│ JavaScript → Logic + API calls          │
│ Leaflet.js → Interactive mapping        │
│ Leaflet    → Drawing tools              │
│   Draw                                   │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│           Backend Stack                 │
├─────────────────────────────────────────┤
│ Python 3.9+   → Core language           │
│ Flask         → Web framework           │
│ Flask-CORS    → Cross-origin requests   │
│ Together SDK  → AI vision API           │
│ python-dotenv → Environment variables   │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│             AI/ML                       │
├─────────────────────────────────────────┤
│ Together AI      → Cloud AI platform    │
│ Llama-Vision-Free→ Vision model         │
│ Base64 Encoding  → Image transmission   │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│          Data Sources                   │
├─────────────────────────────────────────┤
│ Copernicus    → Satellite imagery       │
│ Sentinel-2    → Earth observation       │
│ Local Storage → Image caching           │
└─────────────────────────────────────────┘
```

## 🎯 Data Flow Summary

```
User Input → Frontend Capture → API Request → Backend Processing
    ↓             ↓                  ↓              ↓
 Draw Box    Store Coords      POST JSON     Load Image
                                    ↓              ↓
                              Validate       Encode Base64
                                    ↓              ↓
                              Call AI API    Build Prompt
                                    ↓              ↓
                              Get Response   Stream Back
                                    ↓              ↓
                              Return JSON    Parse Response
                                    ↓              ↓
                              Update UI      Display Chat
```

## 🌟 Project Highlights

✨ **Clean Architecture:** Clear separation of concerns
✨ **Well-Documented:** Every function explained
✨ **Beginner-Friendly:** Simple, readable code
✨ **Production-Ready:** Error handling and validation
✨ **Extensible:** Easy to add new features
✨ **Modern Stack:** Latest technologies and best practices

---

**Made with care for LauzHack 2025** 🔥🛰️🤖

